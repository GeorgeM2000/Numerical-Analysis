#include <bits/stdc++.h>

// Exact Function
double exact_function(double t){
    //return ((t / 2.0) + 1.25 - (0.25 / exp(2.0*t)));
    //return 2.0*(exp(2.0*t) + t);
   //return 1.0 + (1.0/2.0)*exp(-4.0*t) - (1.0/2.0)*exp(-2.0*t);
}