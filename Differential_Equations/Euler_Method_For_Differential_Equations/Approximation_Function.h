#include <bits/stdc++.h>

// Approximation Function
double approximation_function(double t, double y){
    //return (-1.0*(2.0*y) + 3.0 + t);
    //return (2.0*y) - (4.0*t) + 2.0;
    //return 2.0 - exp(-4.0*t) - (2.0*y);
}